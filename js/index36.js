const firstNumber = 10;
const secondNumber = 20;
const result = firstNumber === secondNumber;

console.log("Both variables have the same value ", result)
console.log("Both variables don't have the same value ", !result);