let mutants = ['Professor X', 'Cyclops', 'Iceman', 'Angel', 'Magneto', 'Beast', 'Phoenix', 'Logan', 'Gambit'];

let nameAndPosition = mutants.forEach(function(mutant, position) {
  console.log(mutant, position);
});