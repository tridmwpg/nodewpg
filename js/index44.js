let text = 'HELLO';
let result = text.charAt(text.length - 1) + text.charAt(text.length - 2) + text.charAt(text.length - 3) + text.charAt(text.length - 4) + text.charAt(text.length - 5);

console.log(result);
console.log(`the final result is: ${result.toLowerCase()}`);